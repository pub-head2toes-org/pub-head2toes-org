# UPDATE 4
Focus on improving the search function in the Norther's console and editor (/fs/get/keyboard.html)

# Instructions
* The current implementation works fine for search example like this: `search /md` 
* Check why the search with asterisk (`%`) is not working. Example: `search /%game`
  * Provide a code fix and add unit tests
* Also check how match command is implemented and add some unit tests. Example `match /md gamepad`
